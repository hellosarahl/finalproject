// src/interfaces/Weather.ts
export interface User {
    age: string;
    gender: string;
    preferences: {
        bottom: boolean;
        shoe: boolean;
        top: boolean;
        hat: boolean;
    };
}
