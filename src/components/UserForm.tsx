import styled from "styled-components";
const UserFormContainer = styled.section`
  margin: 0 auto;
`;

export function UserForm() {
    return <UserFormContainer>User form goes here</UserFormContainer>;
}